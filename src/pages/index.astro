---
import Layout from "../layouts/Layout.astro";
import Button from "@/components/ui/Button.astro";
import Section from "@/components/ui/Section.astro";
import { HeroSlider } from "@/components/home/HeroSlider";
import ContentBlock from "@/components/home/ContentBlock.astro";
import TourGrid from "@/components/home/TourGrid.astro";

const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : import.meta.env.BASE_URL + "/";
---

<Layout title="PuraChata Ecolodge | Experiencias en la Amazonía Peruana">
  <div class="flex flex-col">
    {/* BLOQUE 1 – HERO PRINCIPAL */}
    <section class="relative h-screen min-h-[600px] w-full flex items-center">
      <div class="absolute inset-0 z-0">
        <HeroSlider client:visible />
      </div>

      <div class="container mx-auto px-4 z-10 text-white pt-20">
        <div class="max-w-4xl animate-fade-in-up text-center md:text-left">
          <h1
            class="font-serif text-4xl md:text-6xl lg:text-7xl font-bold leading-tight mb-4 drop-shadow-2xl text-white"
          >
            Experiencias en la Amazonía peruana desde Iquitos
          </h1>
          <p
            class="text-xl md:text-2xl mb-8 font-medium max-w-2xl drop-shadow-xl text-gray-100"
          >
            Alojamiento en la selva y tours responsables en Pacaya Samiria
          </p>
          <div
            class="flex flex-col sm:flex-row gap-4 justify-center md:justify-start"
          >
            <Button
              size="lg"
              className="bg-[var(--color-primary)] hover:bg-[var(--color-primary)]/90 text-white border-none shadow-xl transform hover:scale-105 transition-all"
              href={`${base}experiencias`}
            >
              Ver tours
            </Button>
            <Button
              variant="outline"
              size="lg"
              className="!border-white !text-white hover:!bg-white hover:!text-[var(--color-primary)] w-full sm:w-auto shadow-xl"
              href={`${base}reserva`}
            >
              Reservar ahora
            </Button>
          </div>
        </div>
      </div>
    </section>

    {/* BLOQUE 2 – DESCRIPCIÓN PRINCIPAL */}
    <Section className="bg-white pb-0">
      <div class="max-w-4xl mx-auto text-center space-y-4 mb-8">
        <h2
          class="text-3xl md:text-5xl font-serif text-[var(--color-primary)] leading-tight"
        >
          Experiencias auténticas en la Amazonía
        </h2>
        <div class="h-1 w-24 bg-[var(--color-accent)] mx-auto rounded-full">
        </div>
        <p class="text-xl text-gray-600 font-medium">
          Turismo de naturaleza, conexión y propósito
        </p>
      </div>
    </Section>

    {/* 2.1 Ubicación y Entorno */}
    <Section className="bg-white pt-0">
      <ContentBlock
        title="Ubicación y Entorno"
        subtitle="El corazón de la selva"
        description="Ubicados en plena Amazonía peruana, a lo largo de la carretera Iquitos–Nauta, ofrecemos alojamiento en la selva y experiencias diseñadas para descubrir la biodiversidad amazónica de forma accesible, auténtica y respetuosa con el entorno natural."
        imageSrc={base + "images/home-bungalow-sunset.jpg"}
        imageAlt="Entorno natural de Pura Chata Ecolodge"
        reverse={false}
      />
    </Section>

    {/* 2.2 Experiencias y Tours */}
    <Section className="bg-[var(--color-background-soft)]">
      <ContentBlock
        title="Experiencias y Tours"
        subtitle="Aventura y Descubrimiento"
        description="Diseñamos tours de varios días, full days y experiencias personalizadas, incluyendo expediciones en la Reserva Nacional Pacaya Samiria, navegación por ríos amazónicos, caminatas interpretativas, observación de fauna silvestre y contacto directo con la naturaleza."
        imageSrc={base + "images/card-dolphin.png"}
        imageAlt="Exploración en la selva amazónica"
        reverse={true}
        ctaText="Ver todas las experiencias"
        ctaLink={base + "experiencias"}
      />
    </Section>

    {/* 2.3 Valor Diferencial */}
    <Section className="bg-white">
      <ContentBlock
        title="Valor Diferencial"
        subtitle="Turismo Responsable"
        description="Nuestro enfoque integra turismo responsable, conservación ambiental y apoyo a iniciativas sociales, colaborando con comunidades locales y promoviendo un impacto positivo en la región. Cada experiencia busca generar conciencia, respeto y conexión con la Amazonía."
        imageSrc={base + "images/granito-de-arena.jpg"}
        imageAlt="Interacción con la comunidad local"
        reverse={false}
        ctaText="Conoce nuestro impacto"
        ctaLink={base + "impacto-social"}
      />
    </Section>

    {/* BLOQUE 3 – TOURS DESTACADOS */}
    <Section className="bg-[var(--color-background-soft)]">
      <div class="text-center mb-12">
        <span
          class="text-[var(--color-accent)] font-bold tracking-wider uppercase text-sm block mb-2"
        >
          Aventura
        </span>
        <h2
          class="text-4xl md:text-5xl font-serif text-[var(--color-primary)] mb-6"
        >
          Nuestros tours más solicitados
        </h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Descubre la selva a tu ritmo con nuestras experiencias diseñadas para
          conectar con la naturaleza.
        </p>
      </div>

      <TourGrid />

      <div class="text-center mt-12">
        <a href={`${base}experiencias`}>
          <Button size="lg" variant="outline">
            Explorar todos los tours
          </Button>
        </a>
      </div>
    </Section>

    {/* BLOQUE 4 – EXPERIENCIA Y CONFIANZA */}
    <Section className="bg-white">
      <div class="grid md:grid-cols-2 gap-12 items-center max-w-6xl mx-auto">
        <div
          class="relative h-[500px] rounded-2xl overflow-hidden shadow-2xl order-2 md:order-1"
        >
          <img
            src={base + "images/nosotros-equipo.jpg"}
            alt="Equipo y huéspedes en Pura Chata"
            class="object-cover w-full h-full"
          />
          <div
            class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent flex items-end p-8"
          >
            <p class="text-white text-lg font-medium italic">
              "Una experiencia que transforma la manera de ver el mundo."
            </p>
          </div>
        </div>
        <div class="space-y-8 order-1 md:order-2">
          <h2
            class="text-3xl md:text-5xl font-serif text-[var(--color-primary)] leading-tight"
          >
            Más que un viaje, una experiencia con propósito.
          </h2>
          <p class="text-xl text-gray-700 leading-relaxed">
            Cada visita contribuye al cuidado del entorno natural y al
            desarrollo de proyectos sociales en la región amazónica.
          </p>

          <div class="grid grid-cols-2 gap-6 pt-4">
            <div
              class="bg-[var(--color-background-soft)] p-6 rounded-xl text-center"
            >
              <span
                class="block text-4xl font-serif text-[var(--color-accent)] font-bold mb-2"
                >+500</span
              >
              <span
                class="text-sm text-gray-600 font-bold uppercase tracking-wide"
                >Viajeros Felices</span
              >
            </div>
            <div
              class="bg-[var(--color-background-soft)] p-6 rounded-xl text-center"
            >
              <span
                class="block text-4xl font-serif text-[var(--color-accent)] font-bold mb-2"
                >100%</span
              >
              <span
                class="text-sm text-gray-600 font-bold uppercase tracking-wide"
                >Impacto Local</span
              >
            </div>
          </div>

          <div class="pt-4">
            <Button href={`${base}nosotros`} variant="secondary">
              Conoce nuestra historia
            </Button>
          </div>
        </div>
      </div>
    </Section>

    {/* BLOQUE 5 – LLAMADA FINAL A LA ACCIÓN */}
    <section
      class="relative py-32 flex items-center justify-center text-center text-white overflow-hidden"
    >
      {/* Background Image with Overlay */}
      <div class="absolute inset-0 z-0">
        <img
          src={base + "images/home-bungalow-sunset.jpg"}
          alt="Atardecer en la Amazonía"
          class="w-full h-full object-cover"
        />
        <div class="absolute inset-0 bg-black/70 z-10"></div>
      </div>

      <div class="relative z-20 container mx-auto px-4 max-w-4xl space-y-8">
        <h2
          class="text-4xl md:text-6xl font-serif font-bold leading-tight drop-shadow-2xl text-white"
        >
          ¿Listo para descubrir la Amazonía de una forma auténtica y
          responsable?
        </h2>
        <div class="flex flex-col sm:flex-row gap-6 justify-center pt-8">
          <Button
            size="lg"
            className="bg-white hover:bg-gray-100 text-[var(--color-primary)] border-none shadow-2xl text-lg px-12 py-8 h-auto font-serif"
            href={`${base}contacto`}
          >
            Consultar disponibilidad
          </Button>
          <Button
            variant="outline"
            size="lg"
            className="!border-white !text-white hover:!bg-white/10 shadow-2xl text-lg px-12 py-8 h-auto"
            href={`${base}reserva`}
          >
            Reservar ahora
          </Button>
        </div>
      </div>
    </section>
  </div>
</Layout>
